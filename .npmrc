# @vercel/analytics declares an *optional* peer on @sveltejs/kit, which drags an
# unrelated Svelte/Vite chain into npm's resolver and conflicts with our Vite 5.
# We don't use Svelte, so relax strict peer resolution. This applies on Vercel
# too, keeping local and CI installs identical.
legacy-peer-deps=true
